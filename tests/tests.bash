source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/docker.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/fish.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/fonts.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/git.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/kitty.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/lsp.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/node.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/nvim.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/tmux.bash" 
source "$(dirname $(realpath ${BASH_SOURCE[0]}))/checkhealth/ubuntu.bash" 
