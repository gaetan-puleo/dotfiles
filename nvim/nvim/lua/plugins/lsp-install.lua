-- require'lspinstall'.setup() -- important
-- local required_servers = { "lua", "tailwindcss", "bash", "deno" }

-- local servers = require'lspinstall'.installed_servers()
-- for _, server in pairs(servers) do
  -- require'lspconfig'[server].setup{}
-- end
