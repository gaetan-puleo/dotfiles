-- Yabs = require("yabs")

-- Yabs:setup {
--     build_func = function(cmd) vim.cmd("!"..cmd) end
-- }

-- local Language = Yabs.Language

-- local js = Language:new {
--     name = "javascript",
--     command = function()
--         -- This function doesn't return anything, so no shell commands will be run
--       return "node %"k
--     end
-- }
